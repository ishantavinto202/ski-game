export type HudMetricValues = {
  currentHealth: number;
  maxHealth: number;
  currentScore: number;
  totalDistance: number;
};

export type HudSafeAreaLayout = {
  top: number;
  left: number;
  right: number;
};
